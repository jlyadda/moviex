import React from "react";
import { StyleSheet, Text, View } from "react-native";

const NowShowingScreen = () => {
  return (
    <View>
      <Text>NowShowingScreen</Text>
    </View>
  );
};

export default NowShowingScreen;

const styles = StyleSheet.create({});
